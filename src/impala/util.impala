extern "C" {
	fn sinh(Real) -> Real;
}

fn @euclidean(u: &Position, v: &Position) -> Real {
	let mut sum = zero;
	for i in range(0,3) {
		let d = (*u)(i) - (*v)(i);
		sum += d * d;
	}
	math.sqrt(sum)
}
