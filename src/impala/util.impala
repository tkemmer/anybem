extern "C" {
	fn sqrt(f64) -> f64;
	fn sinh(f64) -> f64;
	fn exp(f64)  -> f64;
}

fn euclidean(u: &[f64 * 3], v: &[f64 * 3]) -> f64 {
	let mut sum = 0.;
	for i:i32 in range(0,3) {
		let d: f64 = (*u)(i) - (*v)(i);
		sum += d * d;
	}
	sqrt(sum)
}
