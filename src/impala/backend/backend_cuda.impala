static platform_intrinsics = cuda_intrinsics;
static device_id = 0; // FIXME

fn @platform_acc() -> Accelerator { cuda_accelerator(device_id) }
fn @alloc_platform(size: i32) -> Buffer { alloc_cuda(device_id, size) }
